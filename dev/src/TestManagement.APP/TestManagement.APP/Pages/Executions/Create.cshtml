@page
@model TestManagement.APP.Pages.Executions.CreateModel
@{
    ViewData["Title"] = "Create Execution";
}

<h1>新規テスト実行情報</h1>

<div class="mb-3">
    <button type="button" class="btn btn-secondary" onclick="history.back();">戻る</button>
</div>

<form method="post">
    <div class="form-group">
        <label asp-for="ExecutedAt">実行日</label>
        <input asp-for="ExecutedAt" class="form-control" type="datetime-local" />
        <span asp-validation-for="ExecutedAt" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Environment">実行環境</label>
        <select asp-for="Environment" class="form-control">
            @if (Model.Environments != null)
            {
                foreach (var env in Model.Environments)
                {
                    if (env.Name == Model.Environment)
                    {
                        <option value="@env.Name" selected>@env.DisplayName</option>
                    }
                    else
                    {
                        <option value="@env.Name">@env.DisplayName</option>
                    }
                }
            }
        </select>
        <span asp-validation-for="Environment" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Revision">リビジョン</label>
        <input asp-for="Revision" class="form-control" />
        <span asp-validation-for="Revision" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <button type="submit" class="btn btn-primary">登録</button>
        <a asp-page="./Index" class="btn btn-secondary">キャンセル</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
